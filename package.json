{
  "name": "hotel-booking-workspace",
  "private": true,
  "scripts": {
    "build": "npm --prefix Hotel-Booking install && npm --prefix Hotel-Booking run build",
    "dev": "npm --prefix Hotel-Booking run dev",
    "preview": "npm --prefix Hotel-Booking run preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.1",
    "react-icons": "^5.6.0",
    "tailwindcss": "^4.3.1"
  }
}
